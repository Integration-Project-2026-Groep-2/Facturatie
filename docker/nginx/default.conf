server_names_hash_bucket_size 128;

server {
    listen 80;
    server_name facturatie.integration-project-2026-groep-2.my.be dev-facturatie.integration-project-2026-groep-2.my.be localhost;
    root /var/www/html;
    index index.php index.html;

    # FOSSBilling URL Rewriting
    location / {
        try_files $uri $uri/ @rewrite;
    }

    # Serve theme favicon at root to avoid 404 when browser requests /favicon.ico
    location = /favicon.ico {
        try_files /themes/huraga/assets/favicon.ico =404;
    }

    location @rewrite {
        rewrite ^/(.*)$ /index.php?_url=/$1;
    }

    # Pass PHP scripts to PHP-FPM (localhost since Nginx and PHP-FPM are in same container)
    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_intercept_errors on;
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_index index.php;
    }

    # Allow access to API.js (needed by frontend)
    location = /library/Api/API.js {
        allow all;
        add_header 'Access-Control-Allow-Origin' '*';
    }

    # Deny access to sensitive FOSSBilling files
    location ~* ^/(data|library|config\.php) {
        deny all;
    }
}
